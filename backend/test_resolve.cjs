const { Client } = require('pg'); const client = new Client({ user: 'postgres', password: 'password', host: 'localhost', port: 5432, database: 'domain_system' }); client.connect().then(async () => { const res = await client.query('SELECT * FROM workflow_config WHERE node_id = \ AND action_type = \', ['006929a4-cc24-4fa2-b676-f3460f79eb58', 'UPDATE']); console.log(res.rows); client.end(); });
